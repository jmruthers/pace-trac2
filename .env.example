# Supabase — copy to repo root `.env` and fill keys from each project: Dashboard → Settings → API
#
# `npm run env:dev` / `npm run env:prod` copy one pair into the active vars below and rewrite
# `.cursor/mcp.json` + run `supabase link`. See docs/database/branch-workflow.md.

# Preview branch (dev-db)
SUPABASE_PROJECT_REF_DEV=ykobfbfgushlrjcnwwkf
VITE_SUPABASE_URL_DEV=https://ykobfbfgushlrjcnwwkf.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY_DEV=

# Parent / production project
SUPABASE_PROJECT_REF_PROD=srqhjwivpohjcjqikjry
VITE_SUPABASE_URL_PROD=https://srqhjwivpohjcjqikjry.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY_PROD=

# Active context (updated by npm run env:dev | env:prod)
SUPABASE_ENV=dev
SUPABASE_PROJECT_REF=
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
